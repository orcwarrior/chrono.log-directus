import React from "react";
import styles from "./CalendarViewport.module.scss";

const CalendarViewport = () => {
    return (
        <div className={styles.viewport}>
            Calendar viewport
        </div>
    );
};

export {CalendarViewport};
